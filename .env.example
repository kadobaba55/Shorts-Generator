# ===========================================
# PRODUCTION ENVIRONMENT VARIABLES
# ===========================================
# Bu dosyayı kopyalayıp .env olarak kaydedin
# ve değerleri kendi bilgilerinizle doldurun.

# Veritabanı Bağlantısı (PostgreSQL)
# Hetzner veya başka bir sunucuda PostgreSQL kurulduktan sonra doldurun
DATABASE_URL="postgresql://kullanici:sifre@localhost:5432/shorts_generator"

# NextAuth Güvenlik Anahtarı
# ÖNEMLİ: Aşağıdaki komutu çalıştırıp çıkan değeri buraya yapıştırın:
# Linux/Mac: openssl rand -base64 32
# Windows PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }) -as [byte[]])
NEXTAUTH_SECRET="buraya-guclu-bir-anahtar-yazin"

# Site URL'si
# Örnek: https://shorts-generator.com
NEXTAUTH_URL="https://sizin-domaininiz.com"

# Domain (www olmadan)
# Örnek: shorts-generator.com
NEXT_PUBLIC_DOMAIN="sizin-domaininiz.com"
